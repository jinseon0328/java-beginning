/* 기본형 데이터타입 05 - boolean
boolean은 true/false 단 2개의 값을 갖는 데이터타입이다.
boolean의 경우, 우리가 변수를 만들어서 쓰기보다는
나중에 배우게 될 연산자의 결과물 혹은 메소드의 결과물로 그대로 쓰는 경우가 많다.
특히 조건식을 필요로 하는 조건문이나 반복문에서 이러한 boolean의 결과값을 자주 쓴다.
*/
public class Ex05Boolean {
    public static void main(String[] args){
        //boolean 데이터타입의 값은
        //true false의 키워드로 지정되어 있다.
        boolean b = true;
        System.out.println("b의 현재값");
        System.out.println(b);
        //이렇게 변수를 만들어서 쓰는 경우가 거의 없다!




    }
